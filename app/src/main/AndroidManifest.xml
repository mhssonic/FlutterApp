<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE." />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flutter"
        tools:targetApi="31">
        <activity
            android:name=".ui.userAuth.Profile.ProfileActivity"
            android:exported="false"
            android:label="@string/title_activity_profile"
            android:theme="@style/Theme.Flutter" />
        <activity
            android:name=".ui.menu.CreateTweetActivity"
            android:exported="false"
            android:label="@string/title_activity_create_tweet"
            android:theme="@style/Theme.Flutter" />
            <activity
            android:name=".ui.userAuth.settings.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.menu.MainMenuActivity"
            android:exported="false" />
        <activity
            android:name=".ui.userAuth.sign_up.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.userAuth.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>